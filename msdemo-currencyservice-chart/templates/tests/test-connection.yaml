apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "msdemo-currencyservice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "msdemo-currencyservice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "msdemo-currencyservice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
