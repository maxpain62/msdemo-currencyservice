apiVersion: v1
kind: Service
metadata:
  name: {{ include "msdemo-currencyservice-chart.fullname" . }}
  labels:
    {{- include "msdemo-currencyservice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "msdemo-currencyservice-chart.selectorLabels" . | nindent 4 }}
